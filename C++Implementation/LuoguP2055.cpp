#include <iostream>
#include <cstring>
#include <cstdio>
#include <algorithm>
#include <fstream>
#define For(x,y) for(int i=x;i<y;i++)
using namespace std;

int T,N;
int stay[1000],match[1000],book[1000],e[1000][1000];

int main(){
  cin >> T;
  for(int z=0;z<T;z++) {
    memset(match,0,sizeof(match));
    memset(book,0,sizeof(book));
    memset(e,0,sizeof(e));
    cin >> N;
    For(1,N+1) cin >>  
  }
}
