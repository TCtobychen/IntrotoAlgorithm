#include <iostream>
#include <cstring>
#include <algorithm>
#define For(x,y) for(int i=x;i<y;i++)
using namespace std;

int ans[20];
int N,R;

int logm(int d, int n)
{
  int k=0;
  while(n>=d)
  {k++;n=n/d;}
  return k;
}

int main()
{
  memset(ans,-1,sizeof(ans));
  cin >> N >> R;
  int =logm(abs(R),abs(N));
  dfs(t+1)
}
